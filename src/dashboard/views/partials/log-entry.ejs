<% const icons={ 'config.update' : '⚙️' , 'command.toggle' : '🔧' , 'leveling.update' : '📊' , 'panel.login' : '🔑'
    , 'default' : '📋' } const logIcon=icons[log.action] || icons['default'] const timeStr=new
    Date(log.createdAt).toLocaleString('fr-FR', { day: '2-digit' , month: '2-digit' , hour: '2-digit' ,
    minute: '2-digit' }) %>
    <div class="log-entry log-entry--<%= log.source %>">
        <span class="log-icon">
            <%= logIcon %>
        </span>
        <div class="log-content">
            <span class="log-action">
                <%= log.action %>
            </span>
            <span class="log-user">par <strong>
                    <%= log.userName %>
                </strong></span>
            <% if (log.details) { %>
                <span class="log-details">
                    <%= log.details.substring(0, 100) %>
                </span>
                <% } %>
        </div>
        <span class="log-time">
            <%= timeStr %>
        </span>
        <span class="log-source-badge log-source--<%= log.source %>">
            <%= log.source %>
        </span>
    </div>